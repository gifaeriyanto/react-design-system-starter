/**
 * ------- GUIDELINES -------
 * This file is useful for exporting all components that can be imported after it becomes a package
 * So we can import it like this:
 * import { componentName } from 'package-name';
 */

export { default as Button } from './button';
